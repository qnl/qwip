[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend="setuptools.build_meta"

[pytest]
log_cli = true
log_cli_level = "DEBUG"

[tool.isort]
profile = "black"
skip_gitignore = true
py_version = 310